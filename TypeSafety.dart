main (){
  var firstDecimal= 1.5;
  num secondDecimal= 2.6;
  bool trueOrFalse= true;
  // firstDecimal = trueOrFalse
  print ("error because doesn't use the right operation should be written firstDecimal == trueOrFalse");
 // firstDecimal ="sara"
 print ("If assign string value for firstDecimal the result is error because doesn't  have the same data type use before");
 //var number =7;
 //firstDecimal =number;
 print ("it is an error because diffrent data types the value is int and data type id double");
 print ("when use data type 'num' it will be working and didn't have an error");
 
 }
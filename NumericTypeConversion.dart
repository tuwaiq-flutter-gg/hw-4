main(){
  const int x=10;
  const double y=3.2;
  //const int multipliedAsIntegers =x*y;
  print("if the data type of multipliedAsIntegers is int they have error, but when change data type to double it will be compile");
  const double multipliedAsIntegers =x*y;
  print("it's compile because the data type of the result is double");
}